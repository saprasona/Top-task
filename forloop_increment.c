#include<stdio.h>
//take a numbers from users and print it in increment order//
int main()
{
    int a,num;
    printf("enter the numbers=");
    scanf("%d",&a);
    for(a=0;a<=1000;a++)
    {
        printf("%d\n",a);
    }
    return 0;
}